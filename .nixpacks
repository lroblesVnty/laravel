{
  "phases": {
    "setup": {
      "nixPkgs": [
        "php",
        "php.packages.composer",
        "phpExtensions.bcmath"
      ]
    },
    "install": {
      "cmds": [
        "composer install --no-dev --no-interaction --prefer-dist"
      ]
    }
  }
}
